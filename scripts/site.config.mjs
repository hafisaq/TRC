// The ONE place the site's public URL lives. Everything SEO-facing
// (canonical link, robots.txt sitemap line, every sitemap.xml entry) is
// stamped with this at build time by scripts/apply-site-url.mjs.
//
// When the new Hostinger account's placeholder URL — and later the
// client's real domain — is known, change this line and rebuild. Nothing
// else to touch.
export const SITE_URL = "https://darkviolet-mouse-380960.hostingersite.com";
